{
  "name": "tumeng-landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'static site - no build step required'",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
